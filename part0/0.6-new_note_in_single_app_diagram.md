```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: status code 201 created
    deactivate server
    Note right of browser: The browser doesn’t reload. The spa.js script adds the new note to the local notes array, redraws the HTML so the note appears instantly, and then sends a POST request to the server.